---
title: "Redis on Vortex IQ"
sidebarTitle: "Redis"
description: "Monitor Redis health, cost and reliability signals, and catch incidents and runaway spend early."
---

{/* GENERATED from the connector capability graph; regenerate with
    connector-data-exports/scripts/generate_mdx_all.py. Authored intro copy
    belongs between the HUMAN markers and survives regeneration. */}

{/* HUMAN:intro */}
Monitor Redis health, cost and reliability signals, and catch incidents and runaway spend early.
{/* /HUMAN:intro */}

No changes are made without the configured approval policy. Read-only operations do not modify the connected system; schedules, access scopes, API usage and data handling remain governed by Vortex IQ controls.

| 32 | 8 | 0 | 0 | 9 |
| --- | --- | --- | --- | --- |
| performance signals | automated checks | prepared fixes | proven workflows | API operations |

## Monitor performance

32 performance signals. Signals with an alert band can raise Nerve Centre alerts; every signal supports a merchant-configured watcher.

| Signal | Outcome | Alert behaviour | What it tracks |
| --- | --- | --- | --- |
| **Clients vs maxclients %** | Run operations | Alert band 70 / 90 | connected_clients / maxclients. Hitting cap rejects new connections ,  drops downstream services. |
| **Cluster Slot Coverage Gap (<16384 slots assigned)** | Run operations | Merchant rule | Redis-distinctive ,  slot coverage gap = some keys unreachable, commands on those slots fail. |
| **Cluster Slots Assigned (of 16384)** | Run operations | Alert band 16384 / 16383 | cluster_slots_ok from CLUSTER INFO. <16384 = some keys unreachable (operations on those slots fail). |
| **Command Latency p95 (ms)** | Customer experience | Alert band 50 / 200 | Redis commands typically sub-ms; p95 >10ms = something pathological (large key, slow Lua script, swap). |
| **Connected Clients Saturation vs Traffic Burst** | Protect revenue | Alert band 70 / 90 | Connected Clients Saturation vs Traffic Burst, broken down by row. |
| **Connections Rejected Due to maxclients** | Run operations | Alert band 70 / 90 | Alerts for Connections Rejected Due to maxclients. |
| **Evicted Keys / minute** | Run operations | Merchant rule | evicted_keys delta ,  Redis at maxmemory dropping keys per maxmemory-policy. Indicates memory pressure. |
| **Eviction Storm (>1k evicted_keys/min sustained)** | Run operations | Merchant rule | Alerts for Eviction Storm (>1k evicted_keys/min sustained). |
| **Keyspace Hit Rate %** | Run operations | Alert band 95 / 80 | keyspace_hits / (keyspace_hits + keyspace_misses) from INFO stats. The Redis-defining metric. |
| **Last Successful Backup (hours ago)** | Run operations | Alert band 24 / 72 | Last successful RDB / AOF backup shipped offsite. For ElastiCache: CloudWatch backup events. |
| **Memory Used vs Maxmemory %** | Run operations | Alert band 70 / 90 | used_memory / maxmemory ,  Redis treats memory as 'disk'. Approaching maxmemory triggers eviction. |
| **Operations per Second (live)** | Run operations | Watch only | instantaneous_ops_per_sec from INFO stats. Redis-distinctive ,  proxy for command throughput. |
| **Redis Session Keys vs Active Ecom Users** | Protect revenue | Merchant rule | Redis-distinctive: when Redis holds sessions, session-key count should track active ecom users. |
| **Rejected Connections (24h)** | Control risk and change | Alert band 0.1 / 1 | rejected_connections from INFO stats ,  clients refused due to maxclients hit. |
| **Replica Lag (seconds)** | Run operations | Alert band 1 / 10 | master_last_io_seconds_ago from INFO replication on each replica. |
| **SLOWLOG Entries (15m)** | Customer experience | Alert band 2 / 10 | Commands exceeding slowlog-log-slower-than threshold (default 10ms). |
| **Slow Commands During Checkout Window (5m)** | Protect revenue | Merchant rule | Slow Commands During Checkout Window (5m), broken down by row. |
| **Slow-Query Rate %** | Customer experience | Alert band 1 / 5 | SLOWLOG entry count in the trailing 15m divided by an estimated command count for that same window (INFO stats instantaneous_ops_per_sec x 900s - Redis's own server-compu |
| **Command Latency p99 (ms)** | Customer experience | Alert band 100 / 500 | Description pending editorial review; the signal is live. |
| **Redis Health Score** | Run operations | Alert band 80 / 70 | Description pending editorial review; the signal is live. |
| **Redis OPS Spike vs Ecom Order Rate** | Protect revenue | Merchant rule | Description pending editorial review; the signal is live. |
| **Blocked Clients (BLPOP / BRPOP / WAIT)** | Run operations | Alert band 20 / 100 | Clients waiting on blocking commands ,  queue-consumer + pub/sub stacks live here. |
| **Command Latency p50 (us)** | Customer experience | Watch only | From LATENCY HISTORY (Redis 7+) or sampling client-side. |
| **Connected Replicas** | Run operations | Alert band 1 / 0 | connected_slaves from INFO replication. |
| **Expired Keys / minute** | Run operations | Watch only | Expired Keys / minute over time. |
| **Last AOF Rewrite Status** | Run operations | Alert band 1 / 0 | aof_last_bgrewrite_status from INFO persistence (ok \| err). |
| **Last RDB Save (minutes ago)** | Run operations | Alert band 30 / 60 | rdb_last_save_time from INFO persistence. |
| **Memory Fragmentation Ratio** | Run operations | Merchant rule | mem_fragmentation_ratio = used_memory_rss / used_memory. >1.5 = OS waste; <1 = swap in progress (BAD). |
| **Top 10 SLOWLOG Commands** | Customer experience | Watch only | Top 10 SLOWLOG Commands, broken down by row. |
| **Total Keys (db0)** | Run operations | Watch only | From INFO keyspace db0:keys=. |
| **Connected Clients** | Run operations | Watch only | Description pending editorial review; the signal is live. |
| **Instance Uptime** | Run operations | Watch only | Description pending editorial review; the signal is live. |

## Audit risks and opportunities

A fix status appears only where the action, inputs, approval, verification and recovery controls are mapped. Candidate remediations are never executable.

| Check | Severity | Outcome | Why it matters | Fix status |
| --- | --- | --- | --- | --- |
| **Connection pool saturation above 90%** | critical | Customer experience | At 90% of the connection pool in use, the database is close to refusing new connections outright. Once it does, every part of the application that needs a fresh database connection, includin | Report only |
| **Disk usage above 90%** | critical | Run operations | A database that runs out of disk stops accepting writes entirely, which for most stores means orders, inventory updates and customer records stop being saved, not just that the database gets | Report only |
| **Query error rate above 1% in last 5 minutes** | critical | Run operations | More than 1 in 100 queries is failing right now. Depending on what those queries do, this can mean orders not saving, pages failing to load product or customer data, or background jobs silen | Report only |
| **Last successful backup older than 72 hours** | high | Run operations | If something goes wrong with this database right now, the most recent point it can be restored to is over 3 days old. Every order, customer record and inventory change since that backup woul | Report only |
| **Replication lag above 10 seconds** | high | Run operations | Anything reading from the replica, reports, dashboards, or read traffic split off the primary for capacity, is now up to 10+ seconds stale. If the primary fails while lag is this high, the r | Report only |
| **Slow-query rate above 5% of total** | high | Customer experience | More than 1 in 20 queries is landing in the slow bucket. That is frequent enough to be a pattern, not noise, and it means a meaningful share of every page load or job that touches this datab | Report only |
| **p95 query latency above 200ms sustained 15m** | high | Customer experience | One in twenty queries against this database is taking over 200ms, sustained for at least 15 minutes, not a brief spike. Any storefront page, checkout step or order sync that depends on this  | Report only |
| **Buffer / cache hit rate below 80%** | medium | Run operations | More than 1 in 5 reads is missing the cache and going to disk instead, which is markedly slower. This shows up as everything the database does feeling incrementally heavier, rather than as o | Report only |

## Automate approved work

The Redis connector exposes **9 read** and **0 write** operations to Vortex IQ workflows. Changes follow the merchant's configured approval policy: the target, proposed change, affected records, risk, reversibility and verification plan are shown before execution.

Workflow templates for this connector arrive once proven against a live account; monitoring and audits above are available now.

---

*Generated from the connector capability graph. Counts reflect the servable registry after alias normalisation and de-duplication, and refresh automatically when the registry changes.*
