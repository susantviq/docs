---
title: "Airtable on Vortex IQ"
sidebarTitle: "Airtable"
description: "Monitor Airtable publishing, engagement and freshness, and catch decay before organic reach erodes."
---

{/* GENERATED from the connector capability graph; regenerate with
    connector-data-exports/scripts/generate_mdx_all.py. Authored intro copy
    belongs between the HUMAN markers and survives regeneration. */}

{/* HUMAN:intro */}
Monitor Airtable publishing, engagement and freshness, and catch decay before organic reach erodes.
{/* /HUMAN:intro */}

No changes are made without the configured approval policy. Read-only operations do not modify the connected system; schedules, access scopes, API usage and data handling remain governed by Vortex IQ controls.

[Connect or manage this source](https://app.vortexiq.ai/workbench/settings/sources) &middot; [How connecting works](/integrations/connector-catalogue)

| **11** | **3** | **Build your own** | **Ready to build yours** | **6** |
| --- | --- | --- | --- | --- |
| performance signals | automated checks | automated fixes | workflows | API operations |

## Monitor performance

11 performance signals. Signals with an alert band can raise Nerve Centre alerts; every signal supports a merchant-configured watcher.

| Signal | Outcome | Alert behaviour | What it tracks |
| --- | --- | --- | --- |
| **Abandoned Findings (>14d no movement)** | Protect revenue | Alert band 5 / 15 | Findings sat in the table with no edit for two weeks - the ones losing money silently. Requires a Last modified time field to detect movement. |
| **Backlog by Status** | Run operations | Watch only | Open records grouped by the Status single-select option. |
| **Blocked Records** | Run operations | Alert band 3 / 8 | Records flagged blocked/impeded in the Status field - the stuck list. |
| **Empty Field Rate %** | Control risk and change | Alert band 2 / 15 | Share of the table's declared fields left blank across sampled records. Airtable omits empty fields from a record's fields map entirely, so a field's absence there is aut |
| **Finding Resolution Rate (90d)** | Run operations | Alert band 75 / 50 | What share of audit findings actually got fixed via the Airtable pipeline. <50% means we are filing faster than the team drains. |
| **Freshness Lag (minutes)** | Run operations | Alert band 60 / 1440 | Minutes since the most recently modified record when a 'Last modified time' field exists, otherwise minutes since the most recently created record (createdTime is always  |
| **Oldest Open (days)** | Run operations | Merchant rule | Age of the oldest still-open record, from the record createdTime. |
| **Open Records** | Run operations | Merchant rule | Records whose Status is not in the done set - top-of-funnel backlog volume. |
| **Records Created (7d)** | Run operations | Merchant rule | New records created in the table over the last 7 days (createdTime). |
| **Stale Records (>30d untouched)** | Run operations | Merchant rule | Open records not modified in 30 days. Requires a Last modified time field on the table. |
| **VortexIQ Findings Open** | Run operations | Merchant rule | Records we filed from audit findings that haven't been resolved yet - the live backlog of things VortexIQ surfaced. |

## Audit risks and opportunities

A fix status appears only where the action, inputs, approval, verification and recovery controls are mapped. Candidate remediations are never executable.

| Check | Severity | Outcome | Why it matters | Fix status |
| --- | --- | --- | --- | --- |
| **Data-source API token invalid or access revoked** | critical | Control risk and change | Once this token fails, VortexIQ stops pulling data from this source entirely, and every card and finding built on it goes stale from that moment, silently, until someone notices the numbers  | Report only |
| **Source not updated in over 24h (stale data)** | high | Run operations | A source that has not updated in over a day means every conclusion drawn from it describes yesterday; the staleness is invisible unless surfaced. | Report only |
| **Required-field empty rate above 15%** | medium | Catalogue quality | When required fields are empty in more than 15 percent of rows, every downstream calculation inherits the gaps and quietly under-reports. | Report only |

### Build your own automated fixes

3 checks report findings on Airtable today. Turn any finding into an automated fix with a Vortex IQ workflow: **13,885 read and write operations across 229 connectors** are available as building blocks, with approval, verification and rollback on every change.

## Automate approved work

Vortex IQ is integrated with **4 read** and **2 write** operations across tableornames, metabas, metabastables, metawhoamis on Airtable. Combine them with anything from the **13,885 operations across 229 connectors** to automate the work in your own words.

Changes follow the merchant's configured approval policy: the target, proposed change, affected records, risk, reversibility and verification plan are shown before execution. Read-only operations do not modify the connected system.

[Create a workflow](https://app.vortexiq.ai/workbench/flows/create?connector=airtable)

<Accordion title="Browse the operations you can build with">

| Resource | Read operations | Write operations |
| --- | --- | --- |
| tableornames | 1 | 2 |
| metabas | 1 | 0 |
| metabastables | 1 | 0 |
| metawhoamis | 1 | 0 |

Signed-in users see the full catalogue in the workflow builder, filtered to the sources they have connected.

</Accordion>

### Ready to build your first Airtable workflow

Pick a trigger, add the operations above as steps, and every step that changes data pauses for your approval. Monitoring and audits are live now and can start any workflow you build.

---

*Generated from the connector capability graph. Counts reflect the servable registry after alias normalisation and de-duplication, and refresh automatically when the registry changes.*
