---
title: "Slack on Vortex IQ"
sidebarTitle: "Slack"
description: "Monitor Slack performance signals, run automated checks, and prepare approved work through Vortex IQ."
---

{/* GENERATED from the connector capability graph; regenerate with
    connector-data-exports/scripts/generate_mdx_all.py. Authored intro copy
    belongs between the HUMAN markers and survives regeneration. */}

{/* HUMAN:intro */}
Monitor Slack performance signals, run automated checks, and prepare approved work through Vortex IQ.
{/* /HUMAN:intro */}

No changes are made without the configured approval policy. Read-only operations do not modify the connected system; schedules, access scopes, API usage and data handling remain governed by Vortex IQ controls.

| 5 | 0 | 0 | 0 | 174 |
| --- | --- | --- | --- | --- |
| performance signals | automated checks | prepared fixes | proven workflows | API operations |

## Monitor performance

5 performance signals. Signals with an alert band can raise Nerve Centre alerts; every signal supports a merchant-configured watcher.

| Signal | Outcome | Alert behaviour | What it tracks |
| --- | --- | --- | --- |
| **Slack Channel Configured** | Run operations | Watch only | Renders 'Connected' when webhook_url is present and matches ^https://hooks.slack.com/, else 'Not connected'. Pure credential-shape check, no API call. |
| **Delivery Channel Type** | Run operations | Watch only | Constant 'Incoming Webhook' - the delivery mechanism for this connector. States plainly how messages reach this channel. |
| **Last Delivery Status** | Control risk and change | Watch only | Result of the most recent send. Degrades to no_data (reason delivery_log_unavailable): no send log is persisted yet, and an incoming webhook has no read API to recover it |
| **Messages Sent (30d)** | Run operations | Watch only | Count of messages delivered in the last 30d. Degrades to no_data (reason not_tracked): an incoming webhook exposes no history/read method, so sent volume cannot be counte |
| **Webhook Host** | Run operations | Watch only | Host parsed from webhook_url (expected hooks.slack.com). Confirms the pasted URL targets Slack, not some other host. |

## Audit risks and opportunities

No automated checks are defined for Slack yet; performance signals above cover the monitoring layer, and checks arrive with the connector's audit module.

## Automate approved work

The Slack connector exposes **80 read** and **94 write** operations to Vortex IQ workflows. Changes follow the merchant's configured approval policy: the target, proposed change, affected records, risk, reversibility and verification plan are shown before execution.

Workflow templates for this connector arrive once proven against a live account; monitoring and audits above are available now.

---

*Generated from the connector capability graph. Counts reflect the servable registry after alias normalisation and de-duplication, and refresh automatically when the registry changes.*
