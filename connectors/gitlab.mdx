---
title: "GitLab on Vortex IQ"
sidebarTitle: "GitLab"
description: "Monitor GitLab health, cost and reliability signals, and catch incidents and runaway spend early."
---

{/* GENERATED from the connector capability graph; regenerate with
    connector-data-exports/scripts/generate_mdx_all.py. Authored intro copy
    belongs between the HUMAN markers and survives regeneration. */}

{/* HUMAN:intro */}
Monitor GitLab health, cost and reliability signals, and catch incidents and runaway spend early.
{/* /HUMAN:intro */}

No changes are made without the configured approval policy. Read-only operations do not modify the connected system; schedules, access scopes, API usage and data handling remain governed by Vortex IQ controls.

| 21 | 3 | 0 | 0 | 5 |
| --- | --- | --- | --- | --- |
| performance signals | automated checks | prepared fixes | proven workflows | API operations |

## Monitor performance

21 performance signals. Signals with an alert band can raise Nerve Centre alerts; every signal supports a merchant-configured watcher.

| Signal | Outcome | Alert behaviour | What it tracks |
| --- | --- | --- | --- |
| **Commits (30d)** | Run operations | Alert band 0 / -50 | Count of commits from /projects/{id}/repository/commits (created_at within 30d) summed across the top 10 projects; change_pct vs the prior 30d drives vc_deploy_cadence. |
| **Median Open MR Age** | Run operations | Alert band 3 / 14 | Median of (now - created_at) in days across all open MRs; drives vc_pr_age_days (lower is better). |
| **Pipeline Success Rate (30d)** | Protect revenue | Alert band 95 / 80 | Pipelines with status=success / all completed pipelines (status in success, failed, canceled) created within 30d, x100, across the top 10 projects. |
| **Active Projects (30d)** | Run operations | Watch only | Count of projects with last_activity_at within the last 30d and archived=false. |
| **Avg Commits / Active Project (30d)** | Run operations | Watch only | 30d commit count / active-projects-30d; concentration of delivery across the fleet. |
| **Daily Commits Trend** | Run operations | Watch only | Commits grouped by day over the last 30d (created_at), summed across the top 10 projects. |
| **Deploy Cadence Trend** | Run operations | Alert band 0 / -50 | Daily count of commits over 30d; value = 30d total, change_pct vs the prior 30d drives vc_deploy_cadence. |
| **Deploy Stall Alert** | Run operations | Alert band 0 / -50 | Commit-count change_pct vs the prior 30d; fires on a stall per the vc_deploy_cadence band. |
| **Failed Pipelines (30d)** | Protect revenue | Watch only | Count of pipelines with status=failed created within the last 30d across the top 10 projects. |
| **MR Age Alert** | Run operations | Alert band 3 / 14 | Median open-MR age in days; fires when it crosses the vc_pr_age_days band (bad at >14d). |
| **Merged MRs (30d)** | Run operations | Watch only | Count of MRs with merged_at within the last 30d (state=merged&order_by=updated_at) across the top 10 projects. |
| **Most Active Project (30d)** | Run operations | Watch only | The project with the most commits in the last 30d; value = commit count, detail = path_with_namespace. |
| **Open Issues** | Run operations | Watch only | Sum of open_issues_count across all projects returned by /projects. |
| **Open Merge Requests** | Run operations | Watch only | Count of open MRs from /projects/{id}/merge_requests?state=opened across the top 10 projects. |
| **Pipeline Health Score** | Run operations | Alert band 90 / 70 | 0-100 composite = 0.4 x pipeline success rate + 0.3 x MR-age score (100 at <=3d, 0 at >=14d) + 0.3 x deploy-cadence score (100 + cadence change_pct, clamped 0-100). |
| **Pipelines Run (30d)** | Run operations | Watch only | Count of pipelines created within the last 30d across the top 10 projects. |
| **Pipelines by Status (30d)** | Protect revenue | Watch only | Pipelines created within the last 30d grouped by status field (success, failed, canceled, skipped, etc.) across the top 10 projects. |
| **Projects** | Run operations | Watch only | Count of projects returned by /projects (membership=true). |
| **Projects by Visibility** | Grow revenue | Watch only | Projects grouped by the visibility field (private, internal, public). |
| **Stale MRs (>14d)** | Run operations | Watch only | Count of open MRs whose age (now - created_at) exceeds 14 days. |
| **Total Stars** | Run operations | Watch only | Sum of star_count across all projects. |

## Audit risks and opportunities

A fix status appears only where the action, inputs, approval, verification and recovery controls are mapped. Candidate remediations are never executable.

| Check | Severity | Outcome | Why it matters | Fix status |
| --- | --- | --- | --- | --- |
| **CI pass rate below 80%** | high | Run operations | More than 1 in 5 CI runs is failing. At this rate, a genuinely broken change is more likely to slip through unnoticed (lost in the normal noise of failures) and developers waste time re-runn | Report only |
| **Delivery cadence dropped more than 50% vs the prior period** | medium | Run operations | Deploys or merges have dropped by more than half compared to the prior period. That usually means fixes and improvements, including fixes for issues this same audit has flagged elsewhere, ar | Report only |
| **Median open pull-request age above 14 days** | medium | Run operations | Half of open pull requests have been waiting more than two weeks. Work that is done but not merged is not delivering any value yet, and the longer a PR sits open, the more it tends to drift  | Report only |

## Automate approved work

The GitLab connector exposes **5 read** and **0 write** operations to Vortex IQ workflows. Changes follow the merchant's configured approval policy: the target, proposed change, affected records, risk, reversibility and verification plan are shown before execution.

Workflow templates for this connector arrive once proven against a live account; monitoring and audits above are available now.

---

*Generated from the connector capability graph. Counts reflect the servable registry after alias normalisation and de-duplication, and refresh automatically when the registry changes.*
