---
title: "Azure DevOps on Vortex IQ"
sidebarTitle: "Azure DevOps"
description: "Monitor Azure DevOps health, cost and reliability signals, and catch incidents and runaway spend early."
---

{/* GENERATED from the connector capability graph; regenerate with
    connector-data-exports/scripts/generate_mdx_all.py. Authored intro copy
    belongs between the HUMAN markers and survives regeneration. */}

{/* HUMAN:intro */}
Monitor Azure DevOps health, cost and reliability signals, and catch incidents and runaway spend early.
{/* /HUMAN:intro */}

No changes are made without the configured approval policy. Read-only operations do not modify the connected system; schedules, access scopes, API usage and data handling remain governed by Vortex IQ controls.

[Connect or manage this source](https://app.vortexiq.ai/workbench/settings/sources) &middot; [How connecting works](/integrations/connector-catalogue)

| **24** | **3** | **Build your own** | **Ready to build yours** | **836** |
| --- | --- | --- | --- | --- |
| performance signals | automated checks | automated fixes | workflows | API operations |

## Monitor performance

24 performance signals. Signals with an alert band can raise Nerve Centre alerts; every signal supports a merchant-configured watcher.

| Signal | Outcome | Alert behaviour | What it tracks |
| --- | --- | --- | --- |
| **Build Success Rate (30d)** | Run operations | Alert band 95 / 80 | Builds with result=succeeded / all completed builds (status=completed) queued within 30d, x100, across the top 10 scanned projects. |
| **Commits (30d)** | Grow revenue | Alert band 0 / -50 | Count of commits from GET .../repositories/{repositoryId}/commits (author.date within 30d) summed across the top 15 scanned repos; change_pct vs the prior 30d drives vc_d |
| **Pipeline Health Score** | Grow revenue | Alert band 90 / 70 | 0-100 composite = 0.4 x build success rate (30d, result=succeeded / completed) + 0.3 x PR-age score (100 at &lt;=3d median open-PR age, 0 at &gt;=14d) + 0.3 x commit-cade |
| **Abandoned PRs (30d)** | Protect revenue | Watch only | Count of pull requests with status=abandoned and closedDate within the last 30d (searchCriteria.status=abandoned) across the top 15 scanned repos. |
| **Avg Build Duration** | Run operations | Watch only | Average of (finishTime - startTime) in milliseconds across status=completed builds queued within the last 30d. |
| **Avg Commits / Active Repo (30d)** | Run operations | Watch only | 30d commit count / count of scanned repos with at least 1 commit in 30d; concentration of delivery across the fleet. |
| **Build Failure Rate Alert** | Run operations | Alert band 95 / 80 | Build success rate (result=succeeded / completed) queued within 30d; fires when it crosses the vc_ci_pass_rate band (bad at &lt;80%). |
| **Builds (30d)** | Run operations | Watch only | Count of builds queued within the last 30d from GET .../build/builds across the top 10 scanned projects. |
| **Canceled Builds (30d)** | Protect revenue | Watch only | Count of builds with result=canceled, queued within the last 30d, across the top 10 scanned projects. |
| **Commits by Repository** | Run operations | Watch only | Commits in the last 30d grouped by repo full name (project/name), top 10 by count. |
| **Completed PRs (30d)** | Grow revenue | Watch only | Count of pull requests with status=completed and closedDate within the last 30d (searchCriteria.status=completed) across the top 15 scanned repos. |
| **Daily Commits Trend** | Control risk and change | Watch only | Commits grouped by day over the last 30d (author.date), summed across the top 15 scanned repos. |
| **Deploy Cadence Trend** | Grow revenue | Alert band 0 / -50 | Daily commit count over the last 30d (author.date), summed across the top 15 scanned repos; value = 30d total, change_pct vs the prior 30d drives vc_deploy_cadence. |
| **Deploy Stall Alert** | Grow revenue | Alert band 0 / -50 | Commit-count change_pct vs the prior 30d; fires on a stall per the vc_deploy_cadence band. |
| **Failed Builds (30d)** | Protect revenue | Watch only | Count of builds with result=failed and status=completed, queued within the last 30d, across the top 10 scanned projects. |
| **Median Open PR Age** | Run operations | Alert band 3 / 14 | Median of (now - creationDate) in days across all status=active pull requests; drives vc_pr_age_days (lower is better). |
| **Most Active Repo (30d)** | Run operations | Watch only | The scanned repo with the most commits in the last 30d; value = commit count, detail = repo full name (project/name). |
| **Open PRs by Repository** | Run operations | Watch only | Open (status=active) pull requests grouped by repo full name (project/name), top 10 by count. |
| **Open Pull Requests** | Grow revenue | Watch only | Count of pull requests with status=active from GET .../pullrequests?searchCriteria.status=active across the top 15 scanned repos. |
| **PR Age Alert** | Run operations | Alert band 3 / 14 | Median open-PR age in days; fires when it crosses the vc_pr_age_days band (bad at &gt;14d). |
| **Projects** | Control risk and change | Watch only | Count of projects returned by GET /{organization}/_apis/projects (state=wellFormed). |
| **Repositories** | Control risk and change | Watch only | Count of repositories returned by GET .../{project}/_apis/git/repositories, summed across the top 10 scanned projects. |
| **Repositories by Project** | Run operations | Watch only | Scanned repositories grouped by their parent project name, top 10 by count. |
| **Stale PRs (&gt;14d)** | Run operations | Watch only | Count of status=active pull requests whose age (now - creationDate) exceeds 14 days. |

## Audit risks and opportunities

A fix status appears only where the action, inputs, approval, verification and recovery controls are mapped. Candidate remediations are never executable.

| Check | Severity | Outcome | Why it matters | Fix status |
| --- | --- | --- | --- | --- |
| **CI pass rate below 80%** | high | Run operations | More than 1 in 5 CI runs is failing. At this rate, a genuinely broken change is more likely to slip through unnoticed (lost in the normal noise of failures) and developers waste time re-runn | Report only |
| **Delivery cadence dropped more than 50% vs the prior period** | medium | Grow revenue | Deploys or merges have dropped by more than half compared to the prior period. That usually means fixes and improvements, including fixes for issues this same audit has flagged elsewhere, ar | Report only |
| **Median open pull-request age above 14 days** | medium | Run operations | Half of open pull requests have been waiting more than two weeks. Work that is done but not merged is not delivering any value yet, and the longer a PR sits open, the more it tends to drift  | Report only |

### Build your own automated fixes

3 checks report findings on Azure DevOps today. Turn any finding into an automated fix with a Vortex IQ workflow: **over 13,000 read and write operations across more than 200 connectors** are available as building blocks, with approval, verification and rollback on every change.

## Automate approved work

Vortex IQ is integrated with **420 read** and **416 write** operations across apidashboarddashboardwidgets, apigitrepositorypullrequestreviewers, apiserviceendpointendpoints, apisymbolrequests, apiauditstreams, apibuildbuilds on Azure DevOps. Combine them with anything from the **over 13,000 operations across more than 200 connectors** to automate the work in your own words.

Changes follow the merchant's configured approval policy: the target, proposed change, affected records, risk, reversibility and verification plan are shown before execution. Read-only operations do not modify the connected system.

[Create a workflow](https://app.vortexiq.ai/workbench/flows/create?connector=azure-devops)

<Accordion title="Browse the operations you can build with">

| Resource | Read operations | Write operations |
| --- | --- | --- |
| apidashboarddashboardwidgets | 2 | 6 |
| apigitrepositorypullrequestreviewers | 2 | 6 |
| apiserviceendpointendpoints | 2 | 6 |
| apisymbolrequests | 2 | 6 |
| apiauditstreams | 2 | 4 |
| apibuildbuilds | 2 | 4 |
| apibuilddefinitions | 2 | 4 |
| apidashboarddashboards | 2 | 4 |

Signed-in users see the full catalogue in the workflow builder, filtered to the sources they have connected.

</Accordion>

### Ready to build your first Azure DevOps workflow

Pick a trigger, add the operations above as steps, and every step that changes data pauses for your approval. Monitoring and audits are live now and can start any workflow you build.

---

*Generated from the connector capability graph. Counts reflect the servable registry after alias normalisation and de-duplication, and refresh automatically when the registry changes.*
