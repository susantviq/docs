---
title: "Amazon Web Services on Vortex IQ"
sidebarTitle: "Amazon Web Services"
description: "Monitor Amazon Web Services health, cost and reliability signals, and catch incidents and runaway spend early."
---

{/* GENERATED from the connector capability graph; regenerate with
    connector-data-exports/scripts/generate_mdx_all.py. Authored intro copy
    belongs between the HUMAN markers and survives regeneration. */}

{/* HUMAN:intro */}
Monitor Amazon Web Services health, cost and reliability signals, and catch incidents and runaway spend early.
{/* /HUMAN:intro */}

No changes are made without the configured approval policy. Read-only operations do not modify the connected system; schedules, access scopes, API usage and data handling remain governed by Vortex IQ controls.

| **20** | **3** | **Build your own** | **Ready to build yours** | **3** |
| --- | --- | --- | --- | --- |
| performance signals | automated checks | automated fixes | workflows | API operations |

## Monitor performance

20 performance signals. Signals with an alert band can raise Nerve Centre alerts; every signal supports a merchant-configured watcher.

| Signal | Outcome | Alert behaviour | What it tracks |
| --- | --- | --- | --- |
| **AWS Spend (30d)** | Run operations | Alert band 10 / 50 | Sum of GetCostAndUsage daily UnblendedCost amounts across all services over the last 30d. Cost Explorer must be enabled. |
| **Cloud Health Score** | Run operations | Alert band 90 / 70 | 0-100 composite from the daily cost matrix: 100 minus 20 when 30d spend is up >10% vsP (40 when >50%), minus 10 when the month-end forecast runs >10% above trailing 30d s |
| **Cost by Service (30d)** | Run operations | Watch only | 30d UnblendedCost grouped by the SERVICE dimension (GetCostAndUsage GroupBy DIMENSION SERVICE); top 10 services. |
| **AWS Account** | Run operations | Watch only | Account ID from STS GetCallerIdentity (caller ARN shown as the card detail). Renders with any valid key pair, no Cost Explorer needed. |
| **AWS Spend (7d)** | Run operations | Watch only | Sum of daily UnblendedCost amounts over the last 7d. |
| **Active Services (30d)** | Run operations | Watch only | Count of distinct SERVICE groups with UnblendedCost > 0 in the last 30d. |
| **Avg Daily Spend (30d)** | Run operations | Watch only | 30d spend / days with any nonzero daily cost bucket. |
| **Daily Spend Trend** | Run operations | Watch only | Daily all-service UnblendedCost totals from GetCostAndUsage over the last 30d. |
| **Forecast Month-End Spend** | Run operations | Watch only | GetCostForecast Total.Amount (UNBLENDED_COST, MONTHLY) from today to the first day of next month. Best effort: needs at least one day of cost history. |
| **Forecast Overrun Alert** | Run operations | Alert band 10 / 50 | (GetCostForecast month-end Total.Amount - trailing 30d spend) / trailing 30d spend x 100; fires per the ai_spend_trend band when the forecast runs hot. |
| **Month-to-Date Spend** | Run operations | Watch only | Sum of daily UnblendedCost amounts from the 1st of the current month to today. |
| **Peak Daily Spend (30d)** | Run operations | Watch only | The single largest daily all-service UnblendedCost total in the last 30d (date shown as the card detail). |
| **Previous 30d Spend** | Run operations | Watch only | Sum of daily UnblendedCost amounts for days 31-60 back (the previous 30d comparison window). |
| **Spend Change vs Previous 30d** | Run operations | Watch only | (current 30d spend - previous 30d spend) / previous 30d spend x 100. |
| **Spend Spike Alert** | Run operations | Alert band 10 / 50 | Spend change vs the prior 30d window ((current - previous) / previous x 100); fires on a spike per the ai_spend_trend band. |
| **Spend Yesterday** | Run operations | Watch only | All-service UnblendedCost total for yesterday's daily bucket (the most recent complete day). |
| **Top 5 Services Share (30d)** | Run operations | Watch only | Sum of the top 5 services' 30d UnblendedCost / total 30d UnblendedCost x 100. |
| **Top Service Concentration (30d)** | Run operations | Watch only | Top service 30d UnblendedCost / total 30d UnblendedCost x 100. |
| **Top Service by Cost (30d)** | Run operations | Watch only | The single largest service by 30d UnblendedCost (service name shown as the card detail). |
| **Weekend Spend Share (30d)** | Control risk and change | Watch only | Saturday + Sunday UnblendedCost / total 30d UnblendedCost x 100. Always-on infrastructure sits near 28.6% (2 of 7 days); much lower means workloads scale down off-hours. |

## Audit risks and opportunities

A fix status appears only where the action, inputs, approval, verification and recovery controls are mapped. Candidate remediations are never executable.

| Check | Severity | Outcome | Why it matters | Fix status |
| --- | --- | --- | --- | --- |
| **API key invalid, expired or lacking the scopes the cards need** | high | Control risk and change | Once this credential fails, VortexIQ stops seeing usage, cost and health data from this platform entirely, and every finding here that depends on it goes stale from that moment, silently, un | Report only |
| **Spend up more than 50% vs the prior period** | high | Run operations | Spend on this platform rose more than 50% versus the prior period. A jump this size is either a deliberate scale-up worth confirming was intentional, or an unbounded process, a stuck job, a  | Report only |
| **Usage dropped more than 50% vs the prior period (silent stall)** | medium | Control risk and change | Usage halving against the prior period is the earliest sign of a silent stall: a broken schedule, an expired credential, or a team that quietly stopped. | Report only |

### Build your own automated fixes

3 checks report findings on Amazon Web Services today. Turn any finding into an automated fix with a Vortex IQ workflow: **13,885 read and write operations across 229 connectors** are available as building blocks, with approval, verification and rollback on every change.

## Automate approved work

Vortex IQ is integrated with **0 read** and **3 write** operations across (getcostandusage)s, (getcostforecast)s, resources on Amazon Web Services. Combine them with anything from the **13,885 operations across 229 connectors** to automate the work in your own words.

Changes follow the merchant's configured approval policy: the target, proposed change, affected records, risk, reversibility and verification plan are shown before execution. Read-only operations do not modify the connected system.

### Ready to build your first Amazon Web Services workflow

Pick a trigger, add the operations above as steps, and every step that changes data pauses for your approval. Monitoring and audits are live now and can start any workflow you build.

---

*Generated from the connector capability graph. Counts reflect the servable registry after alias normalisation and de-duplication, and refresh automatically when the registry changes.*
