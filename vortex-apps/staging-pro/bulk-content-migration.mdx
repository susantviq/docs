---
title: "Bulk Content Migration in StagingPro for BigCommerce"
sidebarTitle: "Bulk Content Migration"
---

Bulk Content Migration is the StagingPro screen you use when the destination environment is empty (or you intend to overwrite most of it) and you want to copy your full BigCommerce catalogue from one environment to another. It runs on a dedicated high-performance migration platform with parallel processing, checkpoint resumability, idempotent upserts, and automatic BigCommerce API rate-limit handling.

## When to use Bulk Content Migration

| Scenario | Use Bulk Content Migration |
| --- | --- |
| New sandbox provisioned, need a copy of Production catalogue | Yes |
| Refreshing a stale Staging environment with current Production data | Yes |
| Standing up a fresh UAT for a major release | Yes |
| Moving one or two products between environments | No, use [selective content migration](/vortex-apps/staging-pro/selective-content-migration) |

The bulk migration options are as follows:

| **Option** | **Notes** |
| --- | --- |
| Add New Only | **For existing items in your destination store:** Only add new items. Anything already in your destination store will be left exactly as it is. Nothing will be updated or deleted. <ul><li>Safe on an already-populated store too: existing items are matched and left completely untouched — including everything belonging to them (variants, images, metafields, storefront assignments), and only the missing ones are added.</li><li>Matched items are reported in the **Matched** column.</li><li>One deliberate exception: matched **pages** are always refreshed from the source, because BigCommerce auto-creates stub pages (Contact Us, Blog) on new stores.</li></ul> ℹ️ This never updates or deletes anything already on the destination, it only creates items that are missing. |
| Update Existing | **Keep your destination store up to date without removing existing products:** <ul><li>Add any items that are missing.</li><li>Update matching products to match your source store (only changed fields are written, so re-runs are fast and idempotent).</li><li>Keep products that exist only in your destination store.</li><li>Matching products will have their options, variants, modifiers and pricing updated to match the source store.</li></ul> **⚠️ This cannot be undone. Live stock levels are never changed.** |
| Make Exact Copy | **Make your destination store an exact copy of your source store:** <ul><li>Add missing items.</li><li>Update matching items.</li><li>Remove products, brands, categories and promotions that exist only in the destination store.</li><li>Storefront assignments on **mapped** storefronts are mirrored too — destination-only assignments are removed (counted in the Deleted column); storefronts outside the mapping are never touched.</li></ul> **⚠️ Product deletion only happens during a full-store migration. This cannot be undone. Live stock levels are never changed.**<br />**Themes are never deleted, old copies are left for you to remove manually.**<br /><br />**Note:** This option is **not** displayed if the destination store is "**Production**" |

> **How existing items are matched:** products are matched by **SKU first, then name** (so editing a SKU on the source no longer creates a duplicate; products with no SKU match by name). Brands, categories, customer groups and similar entities match by name, case-insensitively. The one thing no strategy can do is link a product whose **name and SKU were both** changed on the source since the last run — that arrives as a new product.

The assumption underneath Bulk is "destination should look like source for the entities I selected". If that is not your intent, switch to selective.

## The interface

![Bulk Migration 2](/images/BulkMigration-2.png)

The Bulk screen is a three-step flow:

1. **Configure Storefront environments** — pick the source store and storefront, the destination store and storefront, and (for multi-storefront runs) review the channel mapping. **Get entity counts** previews the totals.
2. **Data Treatment Strategy** — choose Add New Only, Update Existing, or Make Exact Copy (the table above).
3. **Select Entities** — tick the entity groups to move (Product Data, Design & Content, Commerce & Marketing, Store Settings, Order Data, and — when both stores are B2B Edition — B2B Edition Migration), then click **Start Migration**.

Starting opens a **confirmation dialog** summarising source → destination, strategy, storefront scope and entity count. If the destination is your live production store, the dialog shows a red warning with a PRODUCTION chip and requires an explicit "I understand" confirmation. Once submitted, the run detail opens with live progress; you can also monitor it from the Home tab or the [Migration History](/vortex-apps/staging-pro/history-and-rollback) tab.

> **Storefront scoping:** picking a specific source storefront scopes the run to that storefront's data (products, categories, pages, theme & Page Builder, scripts); store-wide data (brands, customers, promotions) always migrates in full. Picking **All Storefronts** migrates everything and requires a saved channel mapping. Entity counts and the Forecast always show whole-store totals, so a storefront-scoped run's actual migrated count can be lower than the forecast — that is expected.

> **<kbd>Channel Mapping:</kbd>** You can map the source store channels with the destination store channels prior to the migration (when both the source and destination stores are multi-storefront enabled) so as to enable you to migrate the entities to corresponding destination channels/storefronts.
>
> To do this, click the source store's channel drop down selector and select all storefronts option, map the channels and click Save mapping.
>
> <Frame>
>   ![Channel Mapping](/images/ChannelMapping.png)
> </Frame>

> "**Get Entity Counts**" button → Optionally get entity counts to preview totals on the entities before you migrate.

> Moreover, "**Check Forecast & ETA**" button gives you the calculated estimate time required to migrate the entities so that you can plan your migration accordingly.

## Product Bulk Migration

Product bulk migration processes each product through multiple component steps:

1. **Base product** creation (name, description, price, SKU)
2. **Options** and **Modifiers** setup
3. **Variants** generation
4. **Images** and **Videos** upload
5. **Reviews**, **Custom Fields**, and **Bulk Pricing Rules**
6. **Complex Rules** and **Related Products** associations
7. **Channel Assignments** and **Metafields** (product and variant level)
8. **Content** (widgets and template associations)
9. **Locale** (multi-language product data via GraphQL)

Products can be migrated in **bulk** (all products) or **selective** (specific product IDs) mode.

A failure in any step is logged but does not block subsequent steps for the same product. Failed components show up in the migration error summary with categorised errors and a CSV download.

## Category bulk migration

Category migration automatically includes parent categories, ensuring that the category hierarchy is preserved correctly.

This helps eliminate the “parent category not found” errors that are commonly encountered with manual or custom-built BigCommerce category imports.

## Store & Storefront Settings

The Store Settings panel mirrors the BigCommerce control panel's own Settings page: the ~25 individually-selectable setting groups are organised into the same sections you know from the admin (Setup, General, Orders, Products, Advanced, Domain, Storefront design), with a select-all master checkbox. Channel-scoped settings (email statuses, customer/order settings, web analytics, currency assignments, SEO, search, robots and so on) apply per storefront using your channel mapping, and each group is reported as copied, skipped, or failed.

## Order Data Migration

Under the **Order Data Migration** section, enable:

- **Anonymise Customer Data**

This option **scrambles customer data** from the source environment before migrating it to the destination environment, ensuring **data security** for testing purposes. It is required whenever orders, customers, or gift certificates are selected — and it also unlocks the B2B entities that carry personal data (companies, addresses, users, super admins).

Orders migrate with their **statuses as-is**, and you can restrict the run to specific order statuses. BigCommerce assigns new order numbers on the destination.

## B2B Edition Migration

When **both** the source and destination stores are on B2B Edition, a **B2B Edition Migration** panel appears with six entity families: Roles & Permissions, Companies (including status, payment configuration, parent/child hierarchy, and price-list links), Company Addresses, Company Users, Super Admins, and Sales Staff Assignments. No separate B2B credentials are needed — see [B2B, coupons, and data dependencies](/vortex-apps/staging-pro/b2b-coupons-and-data) for the prerequisites and details.

> If you want a clean-slate destination instead, delete the destination catalogue first via the BigCommerce control panel, then run Bulk migration.

---

## Entity Dependencies

- Entities with the **info-circle icon** have **dependencies**.<br />Example: _Migrating Products also migrates Brands and Categories._
- Entities **without** the icon have **no dependencies** and can be migrated independently.

---

## Monitoring Migration Progress

During bulk migration, you can monitor progress through:

- **Home tab** for overall migration status per environment (live progress bars)
- **Migration History** tab for the live run detail: phase-by-phase progress, ETA, the Breakdown by Phase table, and the itemised issue log

---