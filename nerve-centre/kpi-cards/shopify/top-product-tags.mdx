---
title: "Top Product Tags, Shopify"
sidebarTitle: "Top Product Tags"
description: "Top Product Tags for Shopify stores. Tracked live in Vortex IQ Nerve Centre. How to read it, why it matters, and how to act on it."
---

**Metrics type:** [Supporting Metrics](/nerve-centre/overview#metrics-types-explained)  •  **Category:** [Ecommerce Platform](/nerve-centre/connectors#connectors-by-type)

## At a glance

{/* HUMAN_AUTHORED:tldr */}
> Top tags applied to active products, ranked by product count. Tags are merchant-defined, free-text strings: a hybrid of category, audience, attribute, and operational flag, all stuffed into one field. The card surfaces which tags are doing the heavy lifting.

| | |
| --- | --- |
| **What it counts** | `GROUP BY tag → COUNT(products)` over `Product.tags`. Each tag attached to a product contributes one to its bucket; a product with 8 tags contributes to 8 buckets. The card displays the top N (typically 20) by count. |
| **API endpoint** | `Admin GraphQL. Product.tags` (returns `[String!]`). Tags are case-preserved at write time but treated as case-insensitive for lookup; "Sale" and "sale" merge in admin search but appear as separate buckets in this card unless you've actively normalised. |
| **What "tag" means in Shopify** | A free-text string the merchant attaches to a product. There is no schema, no required vocabulary, no validation. Common patterns: category synonyms (`women`, `mens`), seasonality (`spring-2026`, `holiday`), audience (`gift-under-50`, `b2b-only`), attributes (`organic`, `vegan`, `gluten-free`), operational flags (`do-not-discount`, `dropship`, `pre-order`). |
| **VAT / tax / shipping / discounts / refunds / cancellations** | Not applicable. Tags describe products, not orders. |
| **Currency** | Not applicable. |
| **Channels / sources** | Not filtered. Tags belong to the product record and apply across all channels. POS-only tags (`shop-pop-up-2026`) and Online-Store-only tags (`web-exclusive`) both contribute. |
| **Status filter** | The card runs on the *active* catalog by default. `DRAFT` and `ARCHIVED` products' tags are excluded so the count reflects shopper-visible inventory. |
| **Tag length and casing** | Shopify limits tags to 255 characters and supports any printable Unicode. Casing is preserved on write (`Spring-Sale` is stored as-is) but Shopify's product search treats it case-insensitively. The card displays exactly what's in the data; if your team mixed `Sale`, `SALE`, and `sale` for the same intent, you'll see three separate buckets here. |
| **Tag count per product** | Shopify allows up to 250 tags per product. Healthy stores stay under 15; stores over 30 usually have an automation app spamming tags. |
| **Time window** | `RT` (real-time snapshot). The card always reflects current tag distribution. |
| **Alert trigger** | None. The card is informational; the actionable signal is unusual top-tag concentration (one tag on 90%+ of products is meaningless) or sudden new-tag spikes (an app firing). |
| **Roles** | owner, marketing, operations |
{/* /HUMAN_AUTHORED:tldr */}

## Calculation

```
GROUP BY product.tags
  WHERE date BETWEEN [period_start, period_end]
```

## Worked example

{/* HUMAN_AUTHORED:worked_example */}
A US multi-channel home-goods brand on Shopify (Advanced plan), 1,840 active products, mixed evergreen + seasonal lines. Snapshot at 12 May 26.

| Rank | Tag | Products | Share of catalog | Likely intent |
| --- | --- | --- | --- | --- |
| 1 | `home` | 1,420 | 77.2% | Top-level category, near-universal |
| 2 | `gift` | 980 | 53.3% | Gift-finder collection backing |
| 3 | `under-50` | 624 | 33.9% | Price-tier filter for navigation |
| 4 | `organic` | 412 | 22.4% | Sustainability filter |
| 5 | `Spring-2026` | 318 | 17.3% | Seasonal launch flag |
| 6 | `bestseller` | 220 | 12.0% | Manually curated (most stale) |
| 7 | `do-not-discount` | 184 | 10.0% | Operational flag for discount apps |
| 8 | `dropship` | 145 | 7.9% | Fulfillment routing flag |
| 9 | `b2b-only` | 96 | 5.2% | Channel-restriction flag |
| 10 | `bestseller-2024` | 84 | 4.6% | Stale tag, candidate for cleanup |
| 11 | `pre-order` | 71 | 3.9% | Active operational state |
| 12 | `vegan` | 68 | 3.7% | Attribute filter |
| 13 | `nyc-popup-2025` | 52 | 2.8% | Defunct event, candidate for cleanup |

Five things to notice:

1. **`home` at 77% is meaningless.** When a tag covers most of the catalog, it's a label, not a filter. The merchant probably tagged everything during initial setup. Either remove it (if it's not powering a collection) or accept it as a no-op. The actionable tags are the ones with 5 to 35% coverage; those carve the catalog into useful slices.
2. **`bestseller` is curated, not data-driven.** 220 products tagged "bestseller" almost certainly means a marketing manager updated a tag list a few times. Real bestseller logic should live in a Smart Collection sorted by sales (Shopify Flow + best-selling Liquid). Manual `bestseller` tags go stale within months. Cross-reference [Top Products by Revenue](/nerve-centre/kpi-cards/shopify/top-products-by-revenue), if 60%+ of "bestseller"-tagged products are NOT in the actual top 50 by revenue, the tag is lying to shoppers.
3. **`bestseller-2024` and `nyc-popup-2025` are tag debt.** Time-stamped tags from past campaigns that nobody removed. Each is meaningless to a 2026 shopper but still firing in collections, search, and email automations. **Quarterly tag audit**, search for year-stamps and event names that have passed; bulk-remove via Shopify's bulk editor or a script.
4. **`do-not-discount` and `dropship` are critical operational tags.** These drive automation: discount apps skip them, fulfillment apps route them. **A typo in these tags is silently expensive**, a product tagged `do not discount` (with spaces) instead of `do-not-discount` (with hyphens) will get discounted because the discount-app's exact-match check fails. Audit operational tags for casing and punctuation drift.
5. **Tag bloat correlates with admin slowness.** Stores with average 30+ tags per product report 30 to 60% slower admin product-search experience and visibly slower autocomplete. The performance ceiling matters: at ~250 tags per product, Shopify's admin starts truncating display and the bulk editor fails silently. Healthy median: 5 to 12 tags per product.
{/* /HUMAN_AUTHORED:worked_example */}

## Sibling cards merchants should reference together

{/* HUMAN_AUTHORED:siblings */}
Tags overlap with structured catalog fields. Pair these to choose the right one for the job:

| Card | Why pair it with Product Tags |
| --- | --- |
| [Product Status](/nerve-centre/kpi-cards/shopify/product-status) | This card runs on `ACTIVE` products only. Tags on drafts/archived are invisible to shoppers but counted toward catalog hygiene; status filter shows the gap. |
| [Products by Type](/nerve-centre/kpi-cards/shopify/products-by-type) | Type is the *structured* alternative to tag-as-category. Many merchants use both; the duplication is usually wasteful. |
| [Products by Vendor](/nerve-centre/kpi-cards/shopify/products-by-vendor) | Vendor is the *structured* supplier field. Tags like `vendor-acme` duplicate this and should be removed. |
| [Total Products](/nerve-centre/shopify/total_products) | The denominator. Tag share is meaningful only against catalog size; 200 products tagged `gift` on a 300-product store is different from 200 on a 5,000-product store. |
| [Top Products by Revenue](/nerve-centre/kpi-cards/shopify/top-products-by-revenue) | Reveals whether curated tags (`bestseller`, `featured`) match actual revenue rank. Mismatch = tag debt. |
| [Inventory Alerts](/nerve-centre/kpi-cards/shopify/products-with-zero-negative-stock) | If a tag drives a collection (`new-in`, `back-in-stock`), inventory state on those products determines whether the tag-driven collection is half-empty. |
| [`bigcommerce.product_tags`](/nerve-centre/kpi-cards/bigcommerce/top-product-tags) | BC has no tags field; closest analogue is *custom fields*. Documentation cross-link. |
| [`adobe_commerce.product_tags`](/nerve-centre/adobe_commerce/product_tags) | Adobe stores tags via *categories* + *custom attributes*. Different shape, same intent. Documentation cross-link. |
{/* /HUMAN_AUTHORED:siblings */}

## Reconciling against the vendor's own dashboard

{/* HUMAN_AUTHORED:reconcile */}
**Where to look in Shopify Admin:**

> [Products → All products](https://admin.shopify.com/products) → click **Filter** → **Tagged with** → select the tag

The product list filtered to a single tag returns the count at the top of the table; that's the directly comparable figure for that tag's row in this card. Shopify Admin does not provide a "top tags by count" chart natively; this card is the easiest way to see the global distribution.

For tag *management* (rename, merge, delete in bulk), use the **Tag Suggestions** dropdown in the product editor or a third-party tag-manager app (e.g. Tag Suggester, Smart Tags). Shopify itself has no merge-tags feature in the standard admin; renaming a tag means re-tagging every affected product.

Other Shopify Admin views adjacent to tags:

- **Online Store → Navigation → Collections → Smart Collections**: where tags drive automatic collection inclusion. Shows which tags are *load-bearing*.
- **Apps → (filter / merchandising apps)**: many apps consume tags as their primary input (search filters, recommendation engines, discount rules).
- **Customer tags**: separate field on `Customer.tags`, not products. Don't confuse the two; this card is products only.

**Why our number may legitimately differ from Shopify Admin:**

| Reason | Direction | Why |
| --- | --- | --- |
| **Status filter** | Ours lower | Vortex IQ counts only `ACTIVE` products; Shopify Admin's tag-filter shows tagged products across all statuses. To match, set the status filter to "Active" in the Shopify Admin product list. |
| **Casing** | Either | Our card buckets exactly as the data is written (`Sale` vs `sale` separately). Shopify Admin's tag-filter dropdown is case-insensitive at search time, so a single search may match multiple buckets we display separately. |
| **Sync lag** | Ours lower for last 5 to 15 minutes | Webhook-driven catalog updates with index lag. Bulk tag operations (re-tagging 1,000 products via CSV or bulk editor) can take 5 to 30 minutes to fully reflect. |
| **Duplicates** | Ours higher if duplication app | Some apps generate duplicate products with identical tags during sync; the tag count rises spuriously. |
| **Encoding** | Either | Tags containing special characters (commas, parentheses) sometimes render differently in admin vs the API. |

**Cross-connector reconciliation:**

Product Tags is a Shopify-internal catalog metric. Cross-connector reconciliation is loose because most other connectors model tags differently:

| Card | Expected relationship | Notes |
| --- | --- | --- |
| [`klaviyo.kl_product_tags`](/nerve-centre/klaviyo/kl_product_tags) | Should mirror Shopify if Klaviyo's catalog feed is on | Klaviyo pulls Shopify product feeds and stores tags as `categories`. Drift indicates a feed-sync delay or filter mismatch. |
| [`google_ads.gads_product_groups`](/nerve-centre/google_ads/gads_product_groups) | Google Shopping product groups can be defined by tags | If Performance Max campaigns segment by tag, these should align; mismatches indicate Merchant Center hasn't re-pulled the feed. |

---

<details>
<summary><em>Documentation cross-reference (for agencies running multiple platforms)</em></summary>

The Product Tags concept is Shopify-specific in shape. BigCommerce uses *custom fields*, Adobe Commerce uses a category-tree + custom attribute model. The intent (free-text categorisation overlay) is similar but the data structures differ.

- [`bigcommerce.product_tags`](/nerve-centre/kpi-cards/bigcommerce/top-product-tags)
- [`adobe_commerce.product_tags`](/nerve-centre/adobe_commerce/product_tags)

</details>
{/* /HUMAN_AUTHORED:reconcile */}

## Known limitations / merchant FAQs

{/* HUMAN_AUTHORED:faqs */}
**Why are similar tags showing as separate buckets ("Sale", "sale", "SALE")?**
Because Shopify preserves casing on write. The card displays exactly what's stored. Shopify's product-search treats tags case-insensitively at lookup time, so a shopper filtering "sale" matches all three, but in the data they're three rows. To consolidate, bulk-rename via the Shopify admin product list (filter by each variant, bulk-edit to one canonical form). The roadmap includes a *case-insensitive tag rollup* view; for now, manual normalisation is the only path.

**Should I delete the `home` tag if it's on 77% of products?**
Probably yes if it's not powering a collection or app rule. A near-universal tag is descriptively meaningless and clutters the bulk-editor UI. Audit *first*, search Shopify admin for "home" in Smart Collections, app rules (search apps, discount apps), and Liquid template logic. If nothing depends on it, bulk-remove. If something does, narrow the dependency first.

**How do I find "tag debt" (stale, time-stamped, defunct tags)?**
Search the card for any tag containing a year (`2024`, `2023`), past event names (`black-friday-2024`, `valentines-2025`), or campaign codes nobody recognises. Tag debt grows quietly; quarterly cleanup is the discipline. A useful Ask Viq query: "list product tags applied to fewer than 5 products" surfaces the long-tail; most of those are typos or one-off campaigns.

**My tags drive Smart Collections, will renaming break them?**
Yes. Smart Collections reference tags by exact match. Renaming `Spring-Sale` to `spring-sale` breaks any collection conditioned on the original casing. Process: (1) update the collection condition to the new tag name first, (2) bulk-rename the products, (3) verify the collection re-populates. Apps that consume tags (search, recommendation, discount) need the same treatment; check each one's config before bulk-renaming.

**Multi-currency stores, do tags differ across currencies?**
No. Tags belong to the product record and apply universally. Markets (Shopify's multi-region feature) doesn't change tag visibility per region; you'd have to use channel-specific publication flags or metafields for region-targeted catalog logic.

**Subscription products, are they tagged automatically?**
Not by Shopify itself. Most subscription apps (Recharge, Bold, Shopify Subscriptions) inject a tag like `subscription` or `subscription-eligible` to support filtering and reporting. The card surfaces these alongside merchant-set tags; you can use the count to verify the subscription app is tagging the expected number of products.

**Shopify Plus vs basic plan, behavioural differences?**
Largely none for tag mechanics. Plus stores can use Shopify Flow to auto-tag based on conditions ("if vendor = X, tag as imported"); this leads to tag growth that basic stores don't experience. B2B Edition (Plus) often introduces dedicated tags (`b2b-only`, `b2b-tier-gold`) to drive price-list eligibility; these can dominate the tag distribution on hybrid stores.

**B2B vs DTC, how should I read the tag distribution?**
Tags often serve dual roles: shopper-facing filters (DTC) and operational flags (B2B). On stores running both, the top of the card is usually DTC merchandising tags and the long-tail is operational. Segment by `b2b-only` to see the B2B subset; the top-tags-on-B2B-only view often reveals merchandising vs operational separation that the blended view obscures.

**Refresh cadence?**
Webhook-driven, near real-time. Tag changes propagate within 5 to 15 minutes. Bulk operations (re-tag 5,000 products) take 5 to 30 minutes.

**The card shows a tag I don't recognise, where did it come from?**
Three usual sources: (1) an app injected it (review apps, sustainability badges, recommendation engines often add tags silently), (2) a CSV import included unintended tags, (3) historical migration left behind tags. The Shopify product list filtered to that tag shows you which products carry it; *Created date* on those products usually reveals the source week.

**Action playbook for tag hygiene:**
1. Pull a tag-frequency export (Ask Viq: "export all product tags with counts as CSV").
2. Identify the cleanup candidates: any tag with a date older than 12 months, any tag on fewer than 3 products, any near-universal tag (>70% of catalog) not powering a collection.
3. Cross-check Smart Collection conditions, app configs, and Liquid template references for each cleanup candidate before deleting.
4. Bulk-rename or bulk-remove via the product list (Shopify's bulk editor handles up to ~50 products per action; for larger ops use a script via the Admin API).
5. Set a quarterly cadence for the same audit. Tag debt accumulates faster than any other catalog problem.
{/* /HUMAN_AUTHORED:faqs */}

---

### Tracked live in Vortex IQ Nerve Centre

*Top Product Tags* is one of hundreds of KPI pulses Vortex IQ tracks across Shopify and 70+ other ecommerce connectors. Nerve Centre runs the detection layer; Vortex Mind investigates the cause when something moves; Ask Viq lets you interrogate any number in plain English.

[Start for free](https://app.vortexiq.ai/login) or [book a demo](https://www.vortexiq.ai/contact-us) to see this metric running on your own data.
