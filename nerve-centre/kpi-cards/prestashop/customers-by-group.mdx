---
title: "Customers by Group, PrestaShop"
sidebarTitle: "Customers by Group"
description: "Customers by Group for PrestaShop stores, tracked live in Vortex IQ Nerve Centre. What it counts, why it matters, and how to act on it."
---

**Card class:** Supporting Metrics  *  **Category:** Customers & Retention  *  **Chart:** `bar_horizontal`

## At a glance

> COUNT(customers) GROUP BY id_default_group; label groups from /api/groups names.

| | |
| --- | --- |
| **What it measures** | COUNT(customers) GROUP BY id_default_group; label groups from /api/groups names. |
| **Format** | `number` |
| **Time window** | `RT` |
| **Alert trigger** | `-` |
| **Roles** | owner, marketing |
| **Data type** | Native |

## Calculation

```
COUNT(customers) GROUP BY id_default_group; label groups from /api/groups names.
```

PrestaShop order classification uses the order-state machine: paid = `current_state` in (2 Payment accepted, 3 Processing, 4 Shipped, 5 Delivered, 9 Backorder paid, 11 Remote payment accepted); shipped/fulfilled = (4, 5); refunded = (7, 15); cancelled = (6); payment error = (8). Read live from the PrestaShop Webservice API and snapshotted nightly into the Nerve Centre KPI store.

## Why it matters

{/* HUMAN_AUTHORED:why */}
Customers by Group is part of the PrestaShop executive pulse in Vortex IQ Nerve Centre. Vortex Mind investigates automatically when this card breaches its alert band, and Ask Viq can explain the movement in plain English with a citation back to this card.
{/* /HUMAN_AUTHORED:why */}
