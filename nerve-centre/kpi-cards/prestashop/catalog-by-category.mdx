---
title: "Catalog by Category, PrestaShop"
sidebarTitle: "Catalog by Category"
description: "Catalog by Category for PrestaShop stores, tracked live in Vortex IQ Nerve Centre. What it counts, why it matters, and how to act on it."
---

**Card class:** Supporting Metrics  *  **Category:** Catalog & Inventory  *  **Chart:** `bar_horizontal`

## At a glance

> categories.nb_products_recursive per category WHERE active = 1, top buckets by product count.

| | |
| --- | --- |
| **What it measures** | categories.nb_products_recursive per category WHERE active = 1, top buckets by product count. |
| **Format** | `number` |
| **Time window** | `RT` |
| **Alert trigger** | `-` |
| **Roles** | owner, operations, marketing |
| **Data type** | Native |

## Calculation

```
categories.nb_products_recursive per category WHERE active = 1, top buckets by product count.
```

PrestaShop order classification uses the order-state machine: paid = `current_state` in (2 Payment accepted, 3 Processing, 4 Shipped, 5 Delivered, 9 Backorder paid, 11 Remote payment accepted); shipped/fulfilled = (4, 5); refunded = (7, 15); cancelled = (6); payment error = (8). Read live from the PrestaShop Webservice API and snapshotted nightly into the Nerve Centre KPI store.

## Why it matters

{/* HUMAN_AUTHORED:why */}
Catalog by Category is part of the PrestaShop executive pulse in Vortex IQ Nerve Centre. Vortex Mind investigates automatically when this card breaches its alert band, and Ask Viq can explain the movement in plain English with a citation back to this card.
{/* /HUMAN_AUTHORED:why */}
