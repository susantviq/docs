---
title: "Total Orders, PrestaShop"
sidebarTitle: "Total Orders"
description: "Total Orders for PrestaShop stores, tracked live in Vortex IQ Nerve Centre. What it counts, why it matters, and how to act on it."
---

**Card class:** 🦸 Hero  *  **Category:** Revenue & Growth  *  **Chart:** `kpi`

## At a glance

> COUNT(orders) WHERE current_state IN (2,3,4,5,9,11), windowed on date_add.

| | |
| --- | --- |
| **What it measures** | COUNT(orders) WHERE current_state IN (2,3,4,5,9,11), windowed on date_add. |
| **Format** | `number` |
| **Time window** | `T/7D/30D vsP` |
| **Alert trigger** | `drop >20% vsP`, driven by `sentiment_key: order_count_trend` |
| **Alert band** | trend-based - good when change >= 0%, bad when &lt;= -10% |
| **Roles** | owner, finance, operations |
| **Data type** | Native |

## Calculation

```
COUNT(orders) WHERE current_state IN (2,3,4,5,9,11), windowed on date_add.
```

PrestaShop order classification uses the order-state machine: paid = `current_state` in (2 Payment accepted, 3 Processing, 4 Shipped, 5 Delivered, 9 Backorder paid, 11 Remote payment accepted); shipped/fulfilled = (4, 5); refunded = (7, 15); cancelled = (6); payment error = (8). Read live from the PrestaShop Webservice API and snapshotted nightly into the Nerve Centre KPI store.

## Why it matters

{/* HUMAN_AUTHORED:why */}
Total Orders is part of the PrestaShop executive pulse in Vortex IQ Nerve Centre. Vortex Mind investigates automatically when this card breaches its alert band, and Ask Viq can explain the movement in plain English with a citation back to this card.
{/* /HUMAN_AUTHORED:why */}
