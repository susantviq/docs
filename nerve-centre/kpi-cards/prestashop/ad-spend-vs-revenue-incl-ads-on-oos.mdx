---
title: "Ad Spend vs Revenue (incl. ads on OOS), PrestaShop"
sidebarTitle: "Ad Spend vs Revenue (incl. ads on OOS)"
description: "Ad Spend vs Revenue (incl. ads on OOS) for PrestaShop stores, tracked live in Vortex IQ Nerve Centre. What it counts, why it matters, and how to act on it."
---

**Card class:** 🔗 Cross-Channel  *  **Category:** Cross-Channel: Revenue at Risk  *  **Chart:** `bar_horizontal`

## At a glance

| | |
| --- | --- |
| **What it measures** | Ad Spend vs Revenue (incl. ads on OOS) |
| **Format** | `currency` |
| **Time window** | `30D vsP` |
| **Alert trigger** | `>$0/day spend on OOS SKU`, driven by `sentiment_key: out_of_stock_count` |
| **Sensitivity band** | lower is better - good at/below 0, bad at/above 5 |
| **Roles** | owner, marketing, finance |
| **Data type** | Derived |

## Calculation

```
Ad Spend vs Revenue (incl. ads on OOS)
```

PrestaShop order classification uses the order-state machine: paid = `current_state` in (2 Payment accepted, 3 Processing, 4 Shipped, 5 Delivered, 9 Backorder paid, 11 Remote payment accepted); shipped/fulfilled = (4, 5); refunded = (7, 15); cancelled = (6); payment error = (8). Read live from the PrestaShop Webservice API and snapshotted nightly into the Nerve Centre KPI store.

## Why it matters

{/* HUMAN_AUTHORED:why */}
Ad Spend vs Revenue (incl. ads on OOS) is part of the PrestaShop executive pulse in Vortex IQ Nerve Centre. Vortex Mind investigates automatically when this card breaches its sensitivity band, and Ask Viq can explain the movement in plain English with a citation back to this card.
{/* /HUMAN_AUTHORED:why */}

This is a cross-channel card: it lights up only when the PrestaShop store is paired with the relevant sibling connector (ad platform, email, or payment gateway), joining store data with the other source to surface revenue at risk.
