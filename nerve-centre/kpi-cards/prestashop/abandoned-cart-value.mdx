---
title: "Abandoned Cart Value, PrestaShop"
sidebarTitle: "Abandoned Cart Value"
description: "Abandoned Cart Value for PrestaShop stores, tracked live in Vortex IQ Nerve Centre. What it counts, why it matters, and how to act on it."
---

**Card class:** Non-Hero  *  **Category:** Abandoned Carts  *  **Chart:** `kpi`

## At a glance

> SUM(cart line totals from cart_rows x product price) for abandoned carts in window.

| | |
| --- | --- |
| **What it measures** | SUM(cart line totals from cart_rows x product price) for abandoned carts in window. |
| **Format** | `currency` |
| **Time window** | `30D vsP` |
| **Alert trigger** | `-` |
| **Roles** | owner, marketing, finance |
| **Data type** | Derived |

## Calculation

```
SUM(cart line totals from cart_rows x product price) for abandoned carts in window.
```

PrestaShop order classification uses the order-state machine: paid = `current_state` in (2 Payment accepted, 3 Processing, 4 Shipped, 5 Delivered, 9 Backorder paid, 11 Remote payment accepted); shipped/fulfilled = (4, 5); refunded = (7, 15); cancelled = (6); payment error = (8). Read live from the PrestaShop Webservice API and snapshotted nightly into the Nerve Centre KPI store.

## Why it matters

{/* HUMAN_AUTHORED:why */}
Abandoned Cart Value is part of the PrestaShop executive pulse in Vortex IQ Nerve Centre. Vortex Mind investigates automatically when this card breaches its alert band, and Ask Viq can explain the movement in plain English with a citation back to this card.
{/* /HUMAN_AUTHORED:why */}
