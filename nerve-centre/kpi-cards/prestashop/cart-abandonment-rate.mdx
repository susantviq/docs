---
title: "Cart Abandonment Rate, PrestaShop"
sidebarTitle: "Cart Abandonment Rate"
description: "Cart Abandonment Rate for PrestaShop stores, tracked live in Vortex IQ Nerve Centre. What it counts, why it matters, and how to act on it."
---

**Card class:** Supporting Metrics  *  **Category:** Abandoned Carts  *  **Chart:** `gauge`

## At a glance

> abandoned carts / total carts created (carts whose id_cart never appears in orders / all carts) over date_add window.

| | |
| --- | --- |
| **What it measures** | abandoned carts / total carts created (carts whose id_cart never appears in orders / all carts) over date_add window. |
| **Format** | `percent` |
| **Time window** | `30D vsP` |
| **Alert trigger** | `>75%`, driven by `sentiment_key: abandonment_rate` |
| **Alert band** | lower is better - good at/below 65, bad at/above 80 |
| **Roles** | owner, marketing |
| **Data type** | Derived |

## Calculation

```
abandoned carts / total carts created (carts whose id_cart never appears in orders / all carts) over date_add window.
```

PrestaShop order classification uses the order-state machine: paid = `current_state` in (2 Payment accepted, 3 Processing, 4 Shipped, 5 Delivered, 9 Backorder paid, 11 Remote payment accepted); shipped/fulfilled = (4, 5); refunded = (7, 15); cancelled = (6); payment error = (8). Read live from the PrestaShop Webservice API and snapshotted nightly into the Nerve Centre KPI store.

## Why it matters

{/* HUMAN_AUTHORED:why */}
Cart Abandonment Rate is part of the PrestaShop executive pulse in Vortex IQ Nerve Centre. Vortex Mind investigates automatically when this card breaches its alert band, and Ask Viq can explain the movement in plain English with a citation back to this card.
{/* /HUMAN_AUTHORED:why */}
