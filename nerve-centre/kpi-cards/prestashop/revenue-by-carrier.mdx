---
title: "Revenue by Carrier, PrestaShop"
sidebarTitle: "Revenue by Carrier"
description: "Revenue by Carrier for PrestaShop stores, tracked live in Vortex IQ Nerve Centre. What it counts, why it matters, and how to act on it."
---

**Card class:** Supporting Metrics  *  **Category:** Shipping  *  **Chart:** `bar_horizontal`

## At a glance

> SUM(orders.total_paid_tax_incl) GROUP BY orders.id_carrier over paid-state window.

| | |
| --- | --- |
| **What it measures** | SUM(orders.total_paid_tax_incl) GROUP BY orders.id_carrier over paid-state window. |
| **Format** | `currency` |
| **Time window** | `30D` |
| **Alert trigger** | `-` |
| **Roles** | owner, operations |
| **Data type** | API-derived |

## Calculation

```
SUM(orders.total_paid_tax_incl) GROUP BY orders.id_carrier over paid-state window.
```

PrestaShop order classification uses the order-state machine: paid = `current_state` in (2 Payment accepted, 3 Processing, 4 Shipped, 5 Delivered, 9 Backorder paid, 11 Remote payment accepted); shipped/fulfilled = (4, 5); refunded = (7, 15); cancelled = (6); payment error = (8). Read live from the PrestaShop Webservice API and snapshotted nightly into the Nerve Centre KPI store.

## Why it matters

{/* HUMAN_AUTHORED:why */}
Revenue by Carrier is part of the PrestaShop executive pulse in Vortex IQ Nerve Centre. Vortex Mind investigates automatically when this card breaches its alert band, and Ask Viq can explain the movement in plain English with a citation back to this card.
{/* /HUMAN_AUTHORED:why */}
