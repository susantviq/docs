---
title: "Total Revenue, PrestaShop"
sidebarTitle: "Total Revenue"
description: "Total Revenue for PrestaShop stores, tracked live in Vortex IQ Nerve Centre. What it counts, why it matters, and how to act on it."
---

**Card class:** 🦸 Hero  *  **Category:** Revenue & Growth  *  **Chart:** `kpi`

## At a glance

> SUM(orders.total_paid_tax_incl) WHERE current_state IN (2,3,4,5,9,11), windowed on date_add. Paid-states only excludes carts, cancels, errors.

| | |
| --- | --- |
| **What it measures** | SUM(orders.total_paid_tax_incl) WHERE current_state IN (2,3,4,5,9,11), windowed on date_add. Paid-states only excludes carts, cancels, errors. |
| **Format** | `currency` |
| **Time window** | `T/7D/30D vsP` |
| **Alert trigger** | `drop >15% vsP`, driven by `sentiment_key: revenue_trend` |
| **Sensitivity band** | trend-based - good when change >= 0%, bad when <= -5% |
| **Roles** | owner, finance, marketing |
| **Data type** | API-derived |

## Calculation

```
SUM(orders.total_paid_tax_incl) WHERE current_state IN (2,3,4,5,9,11), windowed on date_add. Paid-states only excludes carts, cancels, errors.
```

PrestaShop order classification uses the order-state machine: paid = `current_state` in (2 Payment accepted, 3 Processing, 4 Shipped, 5 Delivered, 9 Backorder paid, 11 Remote payment accepted); shipped/fulfilled = (4, 5); refunded = (7, 15); cancelled = (6); payment error = (8). Read live from the PrestaShop Webservice API and snapshotted nightly into the Nerve Centre KPI store.

## Why it matters

{/* HUMAN_AUTHORED:why */}
Total Revenue is part of the PrestaShop executive pulse in Vortex IQ Nerve Centre. Vortex Mind investigates automatically when this card breaches its sensitivity band, and Ask Viq can explain the movement in plain English with a citation back to this card.
{/* /HUMAN_AUTHORED:why */}
