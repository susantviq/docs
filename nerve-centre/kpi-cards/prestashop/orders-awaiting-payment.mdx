---
title: "Orders Awaiting Payment, PrestaShop"
sidebarTitle: "Orders Awaiting Payment"
description: "Orders Awaiting Payment for PrestaShop stores, tracked live in Vortex IQ Nerve Centre. What it counts, why it matters, and how to act on it."
---

**Card class:** Non-Hero  *  **Category:** Fulfilment & Operations  *  **Chart:** `kpi`

## At a glance

> COUNT(orders WHERE current_state IN (1,10,13,14)) - awaiting check / bank wire / COD / payment.

| | |
| --- | --- |
| **What it measures** | COUNT(orders WHERE current_state IN (1,10,13,14)) - awaiting check / bank wire / COD / payment. |
| **Format** | `number` |
| **Time window** | `RT` |
| **Alert trigger** | `>2x 30D avg` |
| **Roles** | owner, finance, operations |
| **Data type** | Real-time |

## Calculation

```
COUNT(orders WHERE current_state IN (1,10,13,14)) - awaiting check / bank wire / COD / payment.
```

PrestaShop order classification uses the order-state machine: paid = `current_state` in (2 Payment accepted, 3 Processing, 4 Shipped, 5 Delivered, 9 Backorder paid, 11 Remote payment accepted); shipped/fulfilled = (4, 5); refunded = (7, 15); cancelled = (6); payment error = (8). Read live from the PrestaShop Webservice API and snapshotted nightly into the Nerve Centre KPI store.

## Why it matters

{/* HUMAN_AUTHORED:why */}
Orders Awaiting Payment is part of the PrestaShop executive pulse in Vortex IQ Nerve Centre. Vortex Mind investigates automatically when this card breaches its alert band, and Ask Viq can explain the movement in plain English with a citation back to this card.
{/* /HUMAN_AUTHORED:why */}
