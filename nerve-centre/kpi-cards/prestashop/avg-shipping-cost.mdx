---
title: "Avg Shipping Cost, PrestaShop"
sidebarTitle: "Avg Shipping Cost"
description: "Avg Shipping Cost for PrestaShop stores, tracked live in Vortex IQ Nerve Centre. What it counts, why it matters, and how to act on it."
---

**Card class:** Supporting Metrics  *  **Category:** Shipping  *  **Chart:** `kpi`

## At a glance

> AVG(order_carriers.shipping_cost_tax_incl) for order_carriers whose id_order is a paid-state order in window.

| | |
| --- | --- |
| **What it measures** | AVG(order_carriers.shipping_cost_tax_incl) for order_carriers whose id_order is a paid-state order in window. |
| **Format** | `currency` |
| **Time window** | `30D` |
| **Alert trigger** | `-` |
| **Roles** | owner, operations, finance |
| **Data type** | API-derived |

## Calculation

```
AVG(order_carriers.shipping_cost_tax_incl) for order_carriers whose id_order is a paid-state order in window.
```

PrestaShop order classification uses the order-state machine: paid = `current_state` in (2 Payment accepted, 3 Processing, 4 Shipped, 5 Delivered, 9 Backorder paid, 11 Remote payment accepted); shipped/fulfilled = (4, 5); refunded = (7, 15); cancelled = (6); payment error = (8). Read live from the PrestaShop Webservice API and snapshotted nightly into the Nerve Centre KPI store.

## Why it matters

{/* HUMAN_AUTHORED:why */}
Avg Shipping Cost is part of the PrestaShop executive pulse in Vortex IQ Nerve Centre. Vortex Mind investigates automatically when this card breaches its alert band, and Ask Viq can explain the movement in plain English with a citation back to this card.
{/* /HUMAN_AUTHORED:why */}
