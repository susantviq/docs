---
title: "Total Tax Collected, PrestaShop"
sidebarTitle: "Total Tax Collected"
description: "Total Tax Collected for PrestaShop stores, tracked live in Vortex IQ Nerve Centre. What it counts, why it matters, and how to act on it."
---

**Card class:** Non-Hero  *  **Category:** Revenue & Growth  *  **Chart:** `kpi`

## At a glance

> SUM(orders.total_paid_tax_incl - orders.total_paid_tax_excl) over paid-state orders in window.

| | |
| --- | --- |
| **What it measures** | SUM(orders.total_paid_tax_incl - orders.total_paid_tax_excl) over paid-state orders in window. |
| **Format** | `currency` |
| **Time window** | `30D` |
| **Alert trigger** | `-` |
| **Roles** | owner, finance |
| **Data type** | API-derived |

## Calculation

```
SUM(orders.total_paid_tax_incl - orders.total_paid_tax_excl) over paid-state orders in window.
```

PrestaShop order classification uses the order-state machine: paid = `current_state` in (2 Payment accepted, 3 Processing, 4 Shipped, 5 Delivered, 9 Backorder paid, 11 Remote payment accepted); shipped/fulfilled = (4, 5); refunded = (7, 15); cancelled = (6); payment error = (8). Read live from the PrestaShop Webservice API and snapshotted nightly into the Nerve Centre KPI store.

## Why it matters

{/* HUMAN_AUTHORED:why */}
Total Tax Collected is part of the PrestaShop executive pulse in Vortex IQ Nerve Centre. Vortex Mind investigates automatically when this card breaches its sensitivity band, and Ask Viq can explain the movement in plain English with a citation back to this card.
{/* /HUMAN_AUTHORED:why */}
