---
title: "Recoverable Revenue, PrestaShop"
sidebarTitle: "Recoverable Revenue"
description: "Recoverable Revenue for PrestaShop stores, tracked live in Vortex IQ Nerve Centre. What it counts, why it matters, and how to act on it."
---

**Card class:** Supporting Metrics  *  **Category:** Abandoned Carts  *  **Chart:** `kpi`

## At a glance

> Abandoned-cart value attributable to carts with a known id_customer (recoverable via email) - subset of pre_abandoned_cart_value.

| | |
| --- | --- |
| **What it measures** | Abandoned-cart value attributable to carts with a known id_customer (recoverable via email) - subset of pre_abandoned_cart_value. |
| **Format** | `currency` |
| **Time window** | `30D` |
| **Alert trigger** | `>0` |
| **Roles** | owner, marketing, finance |
| **Data type** | Derived |

## Calculation

```
Abandoned-cart value attributable to carts with a known id_customer (recoverable via email) - subset of pre_abandoned_cart_value.
```

PrestaShop order classification uses the order-state machine: paid = `current_state` in (2 Payment accepted, 3 Processing, 4 Shipped, 5 Delivered, 9 Backorder paid, 11 Remote payment accepted); shipped/fulfilled = (4, 5); refunded = (7, 15); cancelled = (6); payment error = (8). Read live from the PrestaShop Webservice API and snapshotted nightly into the Nerve Centre KPI store.

## Why it matters

{/* HUMAN_AUTHORED:why */}
Recoverable Revenue is part of the PrestaShop executive pulse in Vortex IQ Nerve Centre. Vortex Mind investigates automatically when this card breaches its alert band, and Ask Viq can explain the movement in plain English with a citation back to this card.
{/* /HUMAN_AUTHORED:why */}
