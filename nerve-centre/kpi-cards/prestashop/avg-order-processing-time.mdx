---
title: "Avg Order Processing Time, PrestaShop"
sidebarTitle: "Avg Order Processing Time"
description: "Avg Order Processing Time for PrestaShop stores, tracked live in Vortex IQ Nerve Centre. What it counts, why it matters, and how to act on it."
---

**Card class:** Non-Hero  *  **Category:** Fulfilment & Operations  *  **Chart:** `kpi`

## At a glance

> AVG(time from order_histories first paid-state date_add to first shipped-state (4) date_add) per order, hours, in window.

| | |
| --- | --- |
| **What it measures** | AVG(time from order_histories first paid-state date_add to first shipped-state (4) date_add) per order, hours, in window. |
| **Format** | `number` |
| **Time window** | `30D vsP` |
| **Alert trigger** | `>48h` |
| **Roles** | owner, operations |
| **Data type** | Derived |

## Calculation

```
AVG(time from order_histories first paid-state date_add to first shipped-state (4) date_add) per order, hours, in window.
```

PrestaShop order classification uses the order-state machine: paid = `current_state` in (2 Payment accepted, 3 Processing, 4 Shipped, 5 Delivered, 9 Backorder paid, 11 Remote payment accepted); shipped/fulfilled = (4, 5); refunded = (7, 15); cancelled = (6); payment error = (8). Read live from the PrestaShop Webservice API and snapshotted nightly into the Nerve Centre KPI store.

## Why it matters

{/* HUMAN_AUTHORED:why */}
Avg Order Processing Time is part of the PrestaShop executive pulse in Vortex IQ Nerve Centre. Vortex Mind investigates automatically when this card breaches its alert band, and Ask Viq can explain the movement in plain English with a citation back to this card.
{/* /HUMAN_AUTHORED:why */}
