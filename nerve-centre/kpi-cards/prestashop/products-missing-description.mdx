---
title: "Products Missing Description, PrestaShop"
sidebarTitle: "Products Missing Description"
description: "Products Missing Description for PrestaShop stores, tracked live in Vortex IQ Nerve Centre. What it counts, why it matters, and how to act on it."
---

**Card class:** Supporting Metrics  *  **Category:** Catalog & Inventory  *  **Chart:** `kpi`

## At a glance

> COUNT(products WHERE active = 1 AND description multilang value is empty) - fetch products with display including description.

| | |
| --- | --- |
| **What it measures** | COUNT(products WHERE active = 1 AND description multilang value is empty) - fetch products with display including description. |
| **Format** | `number` |
| **Time window** | `RT` |
| **Alert trigger** | `>5% of catalogue`, driven by `sentiment_key: missing_desc` |
| **Alert band** | lower is better - good at/below 5, bad at/above 20 |
| **Roles** | owner, marketing, operations |
| **Data type** | Derived |

## Calculation

```
COUNT(products WHERE active = 1 AND description multilang value is empty) - fetch products with display including description.
```

PrestaShop order classification uses the order-state machine: paid = `current_state` in (2 Payment accepted, 3 Processing, 4 Shipped, 5 Delivered, 9 Backorder paid, 11 Remote payment accepted); shipped/fulfilled = (4, 5); refunded = (7, 15); cancelled = (6); payment error = (8). Read live from the PrestaShop Webservice API and snapshotted nightly into the Nerve Centre KPI store.

## Why it matters

{/* HUMAN_AUTHORED:why */}
Products Missing Description is part of the PrestaShop executive pulse in Vortex IQ Nerve Centre. Vortex Mind investigates automatically when this card breaches its alert band, and Ask Viq can explain the movement in plain English with a citation back to this card.
{/* /HUMAN_AUTHORED:why */}
