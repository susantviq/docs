---
title: "Cancellation Rate, PrestaShop"
sidebarTitle: "Cancellation Rate"
description: "Cancellation Rate for PrestaShop stores, tracked live in Vortex IQ Nerve Centre. What it counts, why it matters, and how to act on it."
---

**Card class:** Non-Hero  *  **Category:** Refunds & Cancellations  *  **Chart:** `gauge`

## At a glance

> COUNT(orders WHERE current_state = 6) / COUNT(all orders) over date_add window.

| | |
| --- | --- |
| **What it measures** | COUNT(orders WHERE current_state = 6) / COUNT(all orders) over date_add window. |
| **Format** | `percent` |
| **Time window** | `30D vsP` |
| **Alert trigger** | `>3%`, driven by `sentiment_key: cancellation_rate` |
| **Alert band** | lower is better - good at/below 1, bad at/above 3 |
| **Roles** | owner, operations, finance |
| **Data type** | API-derived |

## Calculation

```
COUNT(orders WHERE current_state = 6) / COUNT(all orders) over date_add window.
```

PrestaShop order classification uses the order-state machine: paid = `current_state` in (2 Payment accepted, 3 Processing, 4 Shipped, 5 Delivered, 9 Backorder paid, 11 Remote payment accepted); shipped/fulfilled = (4, 5); refunded = (7, 15); cancelled = (6); payment error = (8). Read live from the PrestaShop Webservice API and snapshotted nightly into the Nerve Centre KPI store.

## Why it matters

{/* HUMAN_AUTHORED:why */}
Cancellation Rate is part of the PrestaShop executive pulse in Vortex IQ Nerve Centre. Vortex Mind investigates automatically when this card breaches its alert band, and Ask Viq can explain the movement in plain English with a citation back to this card.
{/* /HUMAN_AUTHORED:why */}
