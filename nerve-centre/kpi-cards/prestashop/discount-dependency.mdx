---
title: "Discount Dependency, PrestaShop"
sidebarTitle: "Discount Dependency"
description: "Discount Dependency for PrestaShop stores, tracked live in Vortex IQ Nerve Centre. What it counts, why it matters, and how to act on it."
---

**Card class:** Supporting Metrics  *  **Category:** Discounts & Promotions  *  **Chart:** `gauge`

## At a glance

> COUNT(orders WHERE total_discounts_tax_incl > 0) / COUNT(paid-state orders) over window.

| | |
| --- | --- |
| **What it measures** | COUNT(orders WHERE total_discounts_tax_incl > 0) / COUNT(paid-state orders) over window. |
| **Format** | `percent` |
| **Time window** | `30D vsP` |
| **Alert trigger** | `>40% of orders discounted`, driven by `sentiment_key: discount_dependency` |
| **Alert band** | lower is better - good at/below 20, bad at/above 40 |
| **Roles** | owner, marketing, finance |
| **Data type** | Derived |

## Calculation

```
COUNT(orders WHERE total_discounts_tax_incl > 0) / COUNT(paid-state orders) over window.
```

PrestaShop order classification uses the order-state machine: paid = `current_state` in (2 Payment accepted, 3 Processing, 4 Shipped, 5 Delivered, 9 Backorder paid, 11 Remote payment accepted); shipped/fulfilled = (4, 5); refunded = (7, 15); cancelled = (6); payment error = (8). Read live from the PrestaShop Webservice API and snapshotted nightly into the Nerve Centre KPI store.

## Why it matters

{/* HUMAN_AUTHORED:why */}
Discount Dependency is part of the PrestaShop executive pulse in Vortex IQ Nerve Centre. Vortex Mind investigates automatically when this card breaches its alert band, and Ask Viq can explain the movement in plain English with a citation back to this card.
{/* /HUMAN_AUTHORED:why */}
