---
title: "Revenue Drop Alert, PrestaShop"
sidebarTitle: "Revenue Drop Alert"
description: "Revenue Drop Alert for PrestaShop stores, tracked live in Vortex IQ Nerve Centre. What it counts, why it matters, and how to act on it."
---

**Card class:** 🦸 Hero  *  **Category:** Nerve Centre  *  **Chart:** `alert_list`

## At a glance

> Rolling SUM(total_paid_tax_incl) for paid-state orders today vs the same-weekday baseline - catches a broken checkout / disconnected gateway before the merchant notices.

| | |
| --- | --- |
| **What it measures** | Rolling SUM(total_paid_tax_incl) for paid-state orders today vs the same-weekday baseline - catches a broken checkout / disconnected gateway before the merchant notices. |
| **Format** | `percent` |
| **Time window** | `T` |
| **Alert trigger** | `today paid-state revenue drop >25% vs same weekday last 4w`, driven by `sentiment_key: revenue_trend` |
| **Alert band** | trend-based - good when change >= 0%, bad when &lt;= -5% |
| **Roles** | owner, finance, marketing |
| **Data type** | Real-time |

## Calculation

```
Rolling SUM(total_paid_tax_incl) for paid-state orders today vs the same-weekday baseline - catches a broken checkout / disconnected gateway before the merchant notices.
```

PrestaShop order classification uses the order-state machine: paid = `current_state` in (2 Payment accepted, 3 Processing, 4 Shipped, 5 Delivered, 9 Backorder paid, 11 Remote payment accepted); shipped/fulfilled = (4, 5); refunded = (7, 15); cancelled = (6); payment error = (8). Read live from the PrestaShop Webservice API and snapshotted nightly into the Nerve Centre KPI store.

## Why it matters

{/* HUMAN_AUTHORED:why */}
Revenue Drop Alert is part of the PrestaShop executive pulse in Vortex IQ Nerve Centre. Vortex Mind investigates automatically when this card breaches its alert band, and Ask Viq can explain the movement in plain English with a citation back to this card.
{/* /HUMAN_AUTHORED:why */}
