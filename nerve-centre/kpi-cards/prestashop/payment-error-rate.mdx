---
title: "Payment Error Rate, PrestaShop"
sidebarTitle: "Payment Error Rate"
description: "Payment Error Rate for PrestaShop stores, tracked live in Vortex IQ Nerve Centre. What it counts, why it matters, and how to act on it."
---

**Card class:** 🦸 Hero  *  **Category:** Refunds & Cancellations  *  **Chart:** `gauge`

## At a glance

> COUNT(orders WHERE current_state = 8) / COUNT(all orders) over date_add window. A spike = broken gateway / checkout config.

| | |
| --- | --- |
| **What it measures** | COUNT(orders WHERE current_state = 8) / COUNT(all orders) over date_add window. A spike = broken gateway / checkout config. |
| **Format** | `percent` |
| **Time window** | `7D vsP` |
| **Alert trigger** | `>5%`, driven by `sentiment_key: payment_error_rate` |
| **Alert band** | lower is better - good at/below 1, bad at/above 5 |
| **Roles** | owner, finance, operations |
| **Data type** | API-derived |

## Calculation

```
COUNT(orders WHERE current_state = 8) / COUNT(all orders) over date_add window. A spike = broken gateway / checkout config.
```

PrestaShop order classification uses the order-state machine: paid = `current_state` in (2 Payment accepted, 3 Processing, 4 Shipped, 5 Delivered, 9 Backorder paid, 11 Remote payment accepted); shipped/fulfilled = (4, 5); refunded = (7, 15); cancelled = (6); payment error = (8). Read live from the PrestaShop Webservice API and snapshotted nightly into the Nerve Centre KPI store.

## Why it matters

{/* HUMAN_AUTHORED:why */}
Payment Error Rate is part of the PrestaShop executive pulse in Vortex IQ Nerve Centre. Vortex Mind investigates automatically when this card breaches its alert band, and Ask Viq can explain the movement in plain English with a citation back to this card.
{/* /HUMAN_AUTHORED:why */}
