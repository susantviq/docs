---
title: "Unfulfilled Orders, PrestaShop"
sidebarTitle: "Unfulfilled Orders"
description: "Unfulfilled Orders for PrestaShop stores, tracked live in Vortex IQ Nerve Centre. What it counts, why it matters, and how to act on it."
---

**Card class:** Key Metrics  *  **Category:** Fulfilment & Operations  *  **Chart:** `kpi`

## At a glance

> COUNT(orders WHERE current_state IN (2,3,9)) - paid but not yet shipped. Aged by date_add for the alert.

| | |
| --- | --- |
| **What it measures** | COUNT(orders WHERE current_state IN (2,3,9)) - paid but not yet shipped. Aged by date_add for the alert. |
| **Format** | `number` |
| **Time window** | `RT` |
| **Alert trigger** | `>10 orders aged >48h`, driven by `sentiment_key: unfulfilled_count` |
| **Alert band** | lower is better - good at/below 10, bad at/above 50 |
| **Roles** | owner, operations |
| **Data type** | Real-time |

## Calculation

```
COUNT(orders WHERE current_state IN (2,3,9)) - paid but not yet shipped. Aged by date_add for the alert.
```

PrestaShop order classification uses the order-state machine: paid = `current_state` in (2 Payment accepted, 3 Processing, 4 Shipped, 5 Delivered, 9 Backorder paid, 11 Remote payment accepted); shipped/fulfilled = (4, 5); refunded = (7, 15); cancelled = (6); payment error = (8). Read live from the PrestaShop Webservice API and snapshotted nightly into the Nerve Centre KPI store.

## Why it matters

{/* HUMAN_AUTHORED:why */}
Unfulfilled Orders is part of the PrestaShop executive pulse in Vortex IQ Nerve Centre. Vortex Mind investigates automatically when this card breaches its alert band, and Ask Viq can explain the movement in plain English with a citation back to this card.
{/* /HUMAN_AUTHORED:why */}
