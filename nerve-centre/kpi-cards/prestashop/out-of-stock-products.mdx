---
title: "Out-of-Stock Products, PrestaShop"
sidebarTitle: "Out-of-Stock Products"
description: "Out-of-Stock Products for PrestaShop stores, tracked live in Vortex IQ Nerve Centre. What it counts, why it matters, and how to act on it."
---

**Card class:** 🦸 Hero  *  **Category:** Catalog & Inventory  *  **Chart:** `kpi`

## At a glance

> COUNT(stock_availables WHERE quantity &lt;= 0) joined to active products (products.active = 1).

| | |
| --- | --- |
| **What it measures** | COUNT(stock_availables WHERE quantity &lt;= 0) joined to active products (products.active = 1). |
| **Format** | `number` |
| **Time window** | `RT` |
| **Alert trigger** | `>0 (any OOS SKU still listed)`, driven by `sentiment_key: out_of_stock_count` |
| **Sensitivity band** | lower is better - good at/below 0, bad at/above 5 |
| **Roles** | owner, operations, marketing |
| **Data type** | Real-time |

## Calculation

```
COUNT(stock_availables WHERE quantity <= 0) joined to active products (products.active = 1).
```

PrestaShop order classification uses the order-state machine: paid = `current_state` in (2 Payment accepted, 3 Processing, 4 Shipped, 5 Delivered, 9 Backorder paid, 11 Remote payment accepted); shipped/fulfilled = (4, 5); refunded = (7, 15); cancelled = (6); payment error = (8). Read live from the PrestaShop Webservice API and snapshotted nightly into the Nerve Centre KPI store.

## Why it matters

{/* HUMAN_AUTHORED:why */}
Out-of-Stock Products is part of the PrestaShop executive pulse in Vortex IQ Nerve Centre. Vortex Mind investigates automatically when this card breaches its sensitivity band, and Ask Viq can explain the movement in plain English with a citation back to this card.
{/* /HUMAN_AUTHORED:why */}
